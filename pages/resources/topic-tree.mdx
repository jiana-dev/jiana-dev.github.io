import { Library } from '../../components';
import { topics, sortPosts } from "../../lib/getAllPosts";

export const meta = {
  "title": "Topic Tree",
  "preview": "A living collection of topics with some of my thoughts, and other links I found inspiring. I hope to keep adding to this resource for you and myself as well.",
  "date": "2021-04-22T13:37:50.417-04:00",
}

export const folder = 'topics';

export default ({ children }) =>  <Library meta={meta} library={sortPosts(topics)} folder={folder}>{children}</Library>
