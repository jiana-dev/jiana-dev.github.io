import { Library } from '../../components';
import { library, sortPosts } from "../../lib/getAllPosts";

export const meta = {
  "title": "Jiana's Library",
  "preview": "A collection of most books I've read with quick summaries and recommendations on who should read them and when.",
  "date": "2021-03-06T14:51:50.417-07:00",
  "readTime": "14.5 min read",
}

export const folder = 'library';

export default ({ children }) =>  <Library meta={meta} library={sortPosts(library)} folder={folder}>{children}</Library>
