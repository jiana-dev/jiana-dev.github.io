import { Library } from '../../components';
import { financeBooks, sortPosts } from "../../lib/getAllPosts";

export const meta = {
  "title": "My Financial Book Notes",
  "preview": "A collection of books I've read on my journey to becoming more financially literate.",
  "date": "2021-03-06T14:51:50.417-07:00",
  "readTime": "7 min read",
}

export const folder = 'finance_books';

export default ({ children }) =>  <Library meta={meta} library={sortPosts(financeBooks)} folder={folder}>{children}</Library>
