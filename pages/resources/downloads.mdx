import { Library } from '../../components';
import { downloads, sortPosts } from "../../lib/getAllPosts";

export const meta = {
  "title": "Cool Things You Can Download",
  "preview": "Stuff I made that you can have, if you want.",
  "date": "2021-04-22T13:58:50.417-04:00",
}

export const folder = 'downloads';

export default ({ children }) =>  <Library meta={meta} library={sortPosts(downloads)} folder={folder}>{children}</Library>
